name: Release

on:
  push:
    tags:
      - 'v*'
      - '[0-9]+.*'

permissions:
  contents: write

jobs:
  build-and-release:
    name: Build and Release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            platform: windows-x64
            script: ./scripts/package-windows.ps1 -BuildDir build -OutDir dist
            artifact: dist/obs-scene-tree-view-windows-x64.zip
            checksum: dist/obs-scene-tree-view-windows-x64.zip.sha256
          - os: ubuntu-24.04
            platform: linux-x86_64
            script: chmod +x ./scripts/package-linux.sh && ./scripts/package-linux.sh build dist
            artifact: dist/obs-scene-tree-view-linux-x86_64.zip
            checksum: dist/obs-scene-tree-view-linux-x86_64.zip.sha256
          - os: macos-14
            platform: macos-universal
            script: chmod +x ./scripts/package-macos.sh && ./scripts/package-macos.sh build dist
            artifact: dist/obs-scene-tree-view-macos.zip
            checksum: dist/obs-scene-tree-view-macos.zip.sha256

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Build Environment (Windows)
        if: matrix.os == 'windows-latest'
        uses: microsoft/setup-msbuild@v2

      - name: Install Dependencies (Linux)
        if: matrix.os == 'ubuntu-24.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake ninja-build pkg-config qt6-base-dev libqt6widgets6

      - name: Install Dependencies (macOS)
        if: matrix.os == 'macos-14'
        run: |
          brew install cmake ninja qt6

      - name: Build Plugin (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          cmake -S . -B build -G "Visual Studio 17 2022" -A x64
          cmake --build build --config Release

      - name: Build Plugin (Linux/macOS)
        if: matrix.os != 'windows-latest'
        run: |
          if [ "${{ matrix.os }}" = "macos-14" ]; then
            cmake -S . -B build -G Ninja -DCMAKE_OSX_ARCHITECTURES="x86_64;arm64" -DCMAKE_OSX_DEPLOYMENT_TARGET="13.0"
          else
            cmake -S . -B build -G Ninja
          fi
          cmake --build build --config Release

      - name: Package Plugin
        shell: bash
        run: ${{ matrix.script }}

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}
          path: |
            ${{ matrix.artifact }}
            ${{ matrix.checksum }}

  create-release:
    name: Create GitHub Release
    needs: build-and-release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download All Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          draft: true
          generate_release_notes: true
          files: |
            artifacts/**/*.zip
            artifacts/**/*.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

